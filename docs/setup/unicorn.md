# Unicorn Engine

Current Drive source: `re/unicorn-2.1.4-...x86_64.whl`.

```bash
./kit.sh install unicorn
```

The wheel is installed offline into an isolated venv and verified with the repository's x86 emulation smoke test.

Default workspace: `/mnt/data/unicorn-kit`.
